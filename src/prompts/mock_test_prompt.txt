You are an experienced university professor.

Generate a mock examination consisting of multiple choice questions only.
Generate EXACTLY 5 MCQs.

Study Material:

{text}

Difficulty Level:

{difficulty}

Instructions:

1. Generate exactly 15 MCQs.
2. Questions must be based only on the provided study material.
3. Each question must have 4 options.
4. Only one option should be correct.
5. Mix conceptual, numerical and application based questions.
6. Avoid duplicate questions.

Return ONLY valid JSON.

Format:

{{
  "mcqs": [
    {{
      "question": "Question text",
      "options": {{
        "A": "Option A",
        "B": "Option B",
        "C": "Option C",
        "D": "Option D"
      }},
      "answer": "A"
    }}
  ]
}}

Do not return markdown.

Do not wrap JSON in code blocks.

Return only valid JSON.
CRITICAL:

Return ONLY valid JSON.

Every string must be closed properly.

Do not include markdown.

Do not include explanations.

Do not truncate output.

Ensure the JSON can be parsed directly using json.loads().